-- Neovide options

---@class NeovideOptions
local M = {}

M = {
   opt = {},
   g = {
      -- neovide_cursor_trail_length = 0, -- disable cursor trailing
      -- neovide_cursor_animation_length = 0, -- disable cursor animation
   },
}

return M
